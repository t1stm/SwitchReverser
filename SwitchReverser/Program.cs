using System.IO;using System.Linq;/*This program is retarded*/args.ToList().ForEach(arg => File.WriteAllText($"{arg.Split(Path.DirectorySeparatorChar).Last()}-out.txt", File.ReadAllLines(arg).Select(line => line.Split("=>")).Aggregate("", (current, split) => current + $"{split[1].Trim().Replace(',', (char)20)} => {split[0].Trim()},\n")));
